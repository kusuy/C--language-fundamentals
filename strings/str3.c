// Take input in string

#include <stdio.h>
int main()
{
    // Declare a string
    char str[10];

    printf("Enter message: ");
    scanf("%s", str);

    printf("Message = %s\n", str);
}